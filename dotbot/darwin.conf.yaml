- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/alacritty/platforms/darwin.yml: .config/alacritty/platforms/darwin.yml
    ~/.config/kitty/platforms/darwin.conf: .config/kitty/platforms/darwin.conf
    ~/.config/shell_gpt/.sgptrc: .config/shell_gpt/.sgptrc.darwin
    ~/.gnupg/gpg-agent.conf: .gnupg/gpg-agent.conf.darwin
