- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/alacritty/alacritty.yml: .config/alacritty/alacritty.yml.darwin
    ~/.gnupg/gpg-agent.conf: .gnupg/gpg-agent.conf.darwin
