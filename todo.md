# To-Do

- Remove sub-modules.

## Desktop Environment

```sh
sudo usermod -aG video carsme
```

```sh
systemctl start --user pipewire-pulse.service
systemctl start --user mako.service
```

- TLP

## Mail

1. Export Proton Mail bridge certificate.
2. `sudo trust anchor --store cert.pem`
